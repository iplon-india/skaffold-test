steps:
- name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: gcloud
  args: [ "deploy", "release", "create" , "grafana-pm-${DATE}-${TIME}", "--delivery-pipeline","grafana-pm", "--region", "us-west2"]

options:
  logging: CLOUD_LOGGING_ONLY